export enum Channels {
  SMS = "sms",
  EMAIL = "email",
  PUSH_NOTIFICATION = "pushNotification",
}

export const SMS = Channels.SMS;
export const EMAIL = Channels.EMAIL;
export const PUSH_NOTIFICATION = Channels.PUSH_NOTIFICATION